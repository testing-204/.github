name: Checking for dispatch
on:
  workflow_call:
  # repository_dispatch:
  #   types: [org-workflow-bot]
    
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
